# Individual contribution report

---

## Thông tin

- Họ và tên: Đào Quang Thái Anh
- Mã học viên: 2A202602987
- Nhóm: Nhóm Bốn (Day 8 - K4-L3B-RAG-Pipeline)
- Repository/branch: `masao1112/K4-L3B-RAG-Pipeline` / branch `thaianh/02987`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Data Collection & Curation (Task 1, 2) | Thu thập 3 tài liệu quy chế gốc VinUni (Đại học, Thạc sĩ, Tiến sĩ) và tuyển chọn 5 bài viết thông tin tuyển sinh, học phí chính thống. | `src/task1_collect_legal_docs.py`, `src/task2_crawl_news.py`, `data/landing/` | Done |
| Markdown Standardization (Task 3) | Chuẩn hóa toàn bộ dữ liệu PDF/JSON sang Markdown chuẩn cấu trúc, định dạng header và siêu dữ liệu; xử lý fallback khi crawler gặp lỗi. | `src/task3_convert_markdown.py`, `data/standardized/` | Done |
| Golden Dataset (Evaluation) | Thiết kế và hoàn thiện bộ 17 câu hỏi - đáp chuẩn (Ground Truth Q&A) kèm ngữ cảnh trích dẫn (`expected_context`) phục vụ đánh giá RAG. | `group_project/evaluation/golden_dataset.json` | Done |
| QA & Acceptance Testing | Kiểm thử toàn diện acceptance tests (`test_acceptance.py`), phát hiện và xử lý lỗi thiếu dữ liệu, lỗi JSON rỗng. | `tests/test_acceptance.py` | Done |
| Teamwork & Handoff | Chuẩn hóa file phân công thành viên `TEAMMATES.md` và phối hợp rà soát luồng pipeline retrieval A/B testing. | `TEAMMATES.md`, `src/task9_retrieval_pipeline.py` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Sử dụng tài liệu quy chế và chính sách đào tạo chính thống từ Trường Đại học VinUniversity thay cho nguồn diễn đàn / mạng xã hội.  
   **Lý do/evidence:** Các văn bản chính sách của VinUni có số hiệu ban hành cụ thể (VU_HT01, VU_HT02, VU_HT03), cấu trúc điều khoản rõ ràng và tính pháp lý cao. Điều này giúp các cặp Q&A trong Golden Dataset có tính kiểm chứng sự thật tuyệt đối (grounded facts), giảm thiểu hoàn toàn thông tin nhiễu hoặc sai lệch.  
   **Trade-off:** Tài liệu có yếu tố song ngữ Anh - Việt và các điều khoản phức tạp, đòi hỏi khâu tiền xử lý và cắt chunk phải cẩn thận để không làm mất ngữ cảnh văn bản.

2. **Quyết định:** Thiết kế bộ Golden Dataset gồm 17 câu hỏi thực tế có trích đoạn `expected_context` nguyên văn từ văn bản gốc thay vì chỉ viết câu trả lời tóm tắt.  
   **Lý do/evidence:** Trong đánh giá RAG bằng framework Ragas, chỉ số `Context Recall` và `Faithfulness` yêu cầu đối chiếu trực tiếp các mệnh đề sự thật với đoạn văn bản ngữ cảnh chuẩn mực. Việc có `expected_context` chi tiết là điều kiện tiên quyết để đo lường chính xác độ bao phủ của bộ truy xuất.  
   **Trade-off:** Tốn nhiều thời gian đọc hiểu và đối chiếu chéo giữa các văn bản quy chế dài, nhưng đổi lại kết quả đo lường A/B Testing có độ tin cậy cao và thuyết phục.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
  - Lệnh test: `pytest tests/test_acceptance.py -q` và `pytest tests/test_contracts.py -q`.
  - Các query đại diện: *"Học phí cử nhân năm học 2026"*, *"Học phí thạc sĩ MBA toàn chương trình"*, *"Số tín chỉ rút môn tối đa"*, *"Tiêu chí tuyển sinh A-ACC và EXCEL"*.
- Kết quả trước/sau nếu có:
  - Trước: `test_acceptance.py` bị FAILED 4/5 bài test do thiếu tin tức JSON, thiếu markdown chuẩn hóa và `golden_dataset.json` bị rỗng.
  - Sau: Vượt qua (PASSED) toàn bộ 4 bài test về dữ liệu và golden dataset (`test_corpus_has_required_legal_documents`, `test_corpus_has_required_news_with_metadata`, `test_standardized_output_covers_both_source_types`, `test_golden_dataset_has_15_grounded_cases`).
- Lỗi đã phát hiện và cách xử lý:
  - Trang web tuyển sinh chặn request crawl tự động $\rightarrow$ Bổ sung cơ chế fallback trích xuất HTML sạch loại bỏ script, style và định dạng Markdown phân cấp.
  - Lỗi `JSONDecodeError` do file golden dataset rỗng $\rightarrow$ Tạo file JSON hợp lệ với 17 testcase hoàn chỉnh.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: Do các văn bản quy chế gốc có một số bảng biểu phức tạp (ví dụ biểu phí tín chỉ, bảng quy đổi chứng chỉ ngoại ngữ), khi OCR sang text có thể bị đứt đoạn định dạng bảng.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Bổ sung thêm các câu hỏi ngoại domain (Out-of-Domain / Trick queries) vào Golden Dataset để kiểm tra và hiệu chuẩn ngưỡng `SCORE_THRESHOLD` cho cơ chế từ chối an toàn (Safe Refusal).

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Đào Quang Thái Anh
